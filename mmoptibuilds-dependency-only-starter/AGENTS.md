# Agent instructions

Read the parent pack's decision register, source of truth, agent orchestration, model routing, and task brief before editing. Current owner instructions override files.

One agent owns a file at a time. Use the least expensive capable model and task-sized context. Do not invent public facts, install speculative dependencies, expose secrets, use production personal data, or deploy without the release gate. Test first, verify after, and write a complete handoff.
